<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appacademia">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppAcademia">
        <activity android:name=".FormCadastro2"></activity>
        <activity android:name=".DetalhesUsuario" />
        <activity android:name=".ListaAtividade" />
        <activity android:name=".CriarAtividade" />
        <activity android:name=".ListaAlunos" />
        <activity android:name=".MenuProfessor" />
        <activity android:name=".MenuAluno" />
        <activity android:name=".Lista" />
        <activity android:name=".FormCadastro" />
        <activity android:name=".FormLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>